import React from 'react'

const Build = ({ name, contact }) => {
    return (
        <div>
            <div>{name}</div>
            <div>{contact}</div>
        </div>
    )
}

export default Build
